# Changelog

> Tool-maintained. Entries below are appended automatically by `/wrap-up` when it confirms a ticket's branch has merged and sets that ticket's Status to `Done` — see `CLAUDE.md`'s Project Management System section. Safe to hand-edit (e.g. to group entries under a release heading) but not required; don't remove the marker comment below, `wrap-up` looks for it to know where to insert.

<!-- wrap-up: insert new entries below this line, newest first -->

*No shipped changes yet.*
